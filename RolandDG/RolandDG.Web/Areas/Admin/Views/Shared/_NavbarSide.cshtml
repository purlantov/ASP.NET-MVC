<div class="col-sm-3 col-lg-2">
    <nav class="navbar navbar-default navbar-fixed-side">
        <!-- normal collapsible navbar markup -->

        <div class="container">
            <div class="navbar-header">
                <button class="navbar-toggle" data-target=".navbar-collapse" data-toggle="collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="admin">Admin Area</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    @*<li class="active">
                        <a href="#">Left (Default Theme)</a>
                    </li>*@
                    <li class="active">
                        <a href="#">Products</a>
                    </li>
                    <li class="dropdown">
                        @*<a class="dropdown-toggle" data-toggle="dropdown" href="#">Printers <b class="caret"></b></a>*@
                        <ul class="dropdown-menu">
                            <li class="dropdown-header">Printers</li>
                            <li>@Ajax.ActionLink("Add product", "AddPrinter", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Edit printer", "Printers", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        @*<a class="dropdown-toggle" data-toggle="dropdown" href="#">Engravers <b class="caret"></b></a>*@
                        <ul class="dropdown-menu">
                            <li class="divider"></li>
                            <li class="dropdown-header">Engravers</li>
                            <li>@Ajax.ActionLink("Add engraver", "AddEngraver", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Edit engraver", "Engravers", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                        </ul>
                    </li>   
                    <li class="dropdown">
                        @*<a class="dropdown-toggle" data-toggle="dropdown" href="#">Engravers <b class="caret"></b></a>*@
                        <ul class="dropdown-menu">
                            <li class="divider"></li>
                            <li class="dropdown-header">Vinyl Cutters</li>
                            <li>@Ajax.ActionLink("Add Vinyl Cutter", "AddCutter", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Edit Vinyl Cutter", "Cutters", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        @*<a class="dropdown-toggle" data-toggle="dropdown" href="#">Engravers <b class="caret"></b></a>*@
                        <ul class="dropdown-menu">
                            <li class="divider"></li>
                            <li class="dropdown-header">Impact Printers</li>
                            <li>@Ajax.ActionLink("Add engraver", "AddEngraver", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Edit engraver", "Engravers", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                        </ul>
                    </li>  
                    <li class="dropdown">
                        @*<a class="dropdown-toggle" data-toggle="dropdown" href="#">Engravers <b class="caret"></b></a>*@
                        <ul class="dropdown-menu">
                            <li class="divider"></li>
                            <li class="dropdown-header">Users (beta)</li>
                            <li>@Ajax.ActionLink("Add user", "AddUser", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                            <li>@Ajax.ActionLink("Edit user", "Users", "Admin", new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "content" })</li>
                        </ul>
                    </li>
                </ul>
                @*<form class="navbar-form navbar-left">
                    <div class="form-group">
                        <input class="form-control" placeholder="Search">
                    </div>
                    <button class="btn btn-default">Search</button>
                </form>
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#">Page 4</a></li>
                </ul>
                <button class="btn btn-default navbar-btn">Button</button>*@
                <p class="navbar-text">
                    Made by
                    <a href="http://github.com/prulantov">Pavel Purlantov</a>
                </p>
            </div>
        </div>

    </nav>
</div>
